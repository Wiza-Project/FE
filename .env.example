# 복사해서 .env.local 로 사용하세요 (git 미추적)

# 브라우저에서 호출할 API 베이스 경로 (개발 중엔 프록시를 타므로 /api 권장)
VITE_API_BASE_URL=/api

# vite dev server 프록시가 붙을 백엔드 주소 (scms-be 기본 포트 9999)
VITE_API_PROXY_TARGET=http://localhost:9999
