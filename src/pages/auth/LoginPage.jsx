/**
 * 로그인 화면 자리.
 *
 * 구현 전에 결정할 것:
 *  - 학생/교직원은 학사 시스템 계정과 연동할 것인가, 별도 계정을 쓸 것인가
 *  - 기업체는 자체 회원가입이 필요한가
 * 이 결정에 따라 화면이 1개일 수도, 유형별로 나뉠 수도 있습니다.
 */
export default function LoginPage() {
  return (
    <section>
      <h2>로그인</h2>
      <p>구현 예정</p>
    </section>
  );
}
